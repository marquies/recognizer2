# Recogizer Application

## Build 

Build the project with the command

> gradle build
                                  
The created jar with dependencies will be created at `build/libs`

## Execution

Executue with ``java -jar recogizer2-VERSION.jar``
                                                 

